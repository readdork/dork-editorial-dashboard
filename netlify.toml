[build]
  command = "echo 'Building with VITE_SUPABASE_URL:' $VITE_SUPABASE_URL && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
